import {
  createParser,
  hashtagParser,
  linkParser,
  brParser,
  emojiParser,
  mentionParser
} from 'js/textParser';

describe('createParser', () => {
  it('works', () => {
    expect(1).toBe(1);
  });
});
